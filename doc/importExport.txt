default 키워드를 붙여서 내보낸 것은 중괄호 없이 불러옴
default export는 가져올 때 이름이 원래 것과 달라도 상관없음

여러 개의 함수(혹은 클래스, 변수)를 가져오고 싶으면 내보낼 것 앞에 모두 export를 붙임
import할 때 { } (중괄호) 안에 가져오고자 하는 것들을 콤마로 여러 개 적음
또는 import * as ...를 사용해서 모두 임포트 가능 